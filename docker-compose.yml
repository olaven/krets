version: '3.0'
services:
    web: 
        build: "."
        ports:
            - "80:8080"
        environment: 
            - AUTH0_CLIENT_ID
            - AUTH0_CLIENT_SECRET 
            - AUTH0_DOMAIN
            - HOST_URI
